% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coil20.R
\name{download_coil100}
\alias{download_coil100}
\title{Download COIL-100}
\format{
If \code{as = "data.frame"}, the default, a data frame with 49,153
variables:
\itemize{
\item \code{r_x1_y1}, \code{r_x1_y2}, ... \code{r_x128_y128}: Pixel values in the red channel.
\item \code{g_x1_y1}, \code{g_x1_y2}, ... \code{g_x128_y128}: Pixel values in the green
channel.
\item \code{b_x1_y1}, \code{b_x1_y2}, ... \code{b_x128_y128}: Pixel values in the blue channel.
\item \code{Label}: The id of the object represented by the image, in the range 1 to
100. Stored as a factor.
}

Pixel values range from 0 to 1. Within each channel, the \code{x} index increases
from left to right, and the \code{y} index increases from top to bottom.

The name of each row is \code{"<object>_<angle>"}, where \verb{<object>} is the object
id and \verb{<angle>} is the viewing angle in degrees, from 0 to 355 in
five-degree increments. For example, \code{"5_150"} is the fifth object viewed at
an angle of 150 degrees.

For more information see \url{https://cave.cs.columbia.edu/repository/COIL-100}.
}
\usage{
download_coil100(
  file = NULL,
  cleanup = TRUE,
  verbose = FALSE,
  as = c("data.frame", "matrix")
)
}
\arguments{
\item{file}{File path to download the zip archive to. If \code{NULL}, a file in a
temporary work directory is used.}

\item{cleanup}{If \code{TRUE}, delete temporary download and extraction files
before returning. If \code{file} is supplied, only that file and the dedicated
extraction directory are removed.}

\item{verbose}{If \code{TRUE}, log download and extraction progress.}

\item{as}{Return format. Use \code{"data.frame"} for the original wide data frame
shape, or \code{"matrix"} for a list with \code{data}, \code{labels}, \code{poses}, and \code{ids}.}
}
\value{
If \code{as = "data.frame"}, a data frame containing the COIL-100 dataset.
If \code{as = "matrix"}, a list containing a numeric matrix with one image per
row, factor labels, integer viewing angles, and row ids.
}
\description{
Download the processed COIL-100 database of object images.
}
\details{
Downloads and expands the zip file of the processed COIL-100 database of
object images and converts the PNG files to a data frame or matrix-list
result. There are 100 objects, each represented by 72 poses, in RGB PNG
format. Each image has a resolution of 128 x 128.
}
